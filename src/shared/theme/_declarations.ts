declare module '@material-ui/core/styles/createPalette' {
  interface Palette {
    border: string;
    gradient: string;
  }

  interface PaletteOptions {
    border?: string;
    gradient?: string;
  }
}
