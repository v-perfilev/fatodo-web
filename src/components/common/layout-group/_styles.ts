import {makeStyles} from '@material-ui/core/styles';
import {Theme} from '@material-ui/core';

export const colorViewStyles = makeStyles(() => ({
  root: {
    width: '100%',
    height: 20,
  },
}));
