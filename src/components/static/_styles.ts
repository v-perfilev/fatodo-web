import {makeStyles} from '@material-ui/core/styles';

export const staticPageStyles = makeStyles(() => ({
  root: {
    textAlign: 'center',
  },
  code: {
    fontSize: '10em',
    lineHeight: 1,
  },
}));
