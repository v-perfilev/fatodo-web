import * as React from 'react';
import {FC} from 'react';
import {Box} from '@material-ui/core';

const NotActivated: FC = () => (
  <Box>Not activated. Check your email box.</Box>
);

export default NotActivated;
