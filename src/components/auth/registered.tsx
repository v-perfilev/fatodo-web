import * as React from 'react';
import {FC} from 'react';
import {Box} from '@material-ui/core';

const Registered: FC = () => (
  <Box>Registered. Check your email for activation.</Box>
);

export default Registered;
