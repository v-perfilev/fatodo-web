import React from 'react';
import {FALLBACK_AVATAR} from '../../constants';

const FallbackPic = () => {
  return <img alt="Fatodo fallback image" src={FALLBACK_AVATAR} />;
};

export default FallbackPic;
